replicaCount: 1

image:
  repository: grafana/loki
  tag: "2.9.0"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 3100

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 250m
    memory: 256Mi

persistence:
  enabled: true
  size: 10Gi
